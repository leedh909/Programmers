SELECT A.CART_ID
FROM(
    SELECT *
    FROM CART_PRODUCTS
    WHERE NAME='Milk') A,
    (
    SELECT *
    FROM CART_PRODUCTS
    WHERE NAME='Yogurt' ) B
WHERE A.CART_ID = B.CART_ID
GROUP BY CART_ID
ORDER BY A.ID